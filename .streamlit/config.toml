# Streamlit theme aligned with the dashboard's chart palette
# (see dashboard/core/plots.py for the chart color roles).
[theme]
base = "light"
primaryColor = "#2a78d6"
backgroundColor = "#fcfcfb"
secondaryBackgroundColor = "#f9f9f7"
textColor = "#0b0b0b"

[browser]
gatherUsageStats = false
